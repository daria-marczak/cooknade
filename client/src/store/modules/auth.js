const state = {
	isLoggedIn: false,
};

const actions = {};

const mutations = {};

const getters = {
	isLoggedIn: state => state.isLoggedIn,
};

export default { state, actions, mutations, getters };
